import React from 'react'

const Filters = (props) => {
  return (
    <>
      <button>Filter</button>
      <button>Filter</button>
      <button>Filter</button>
      <button>Filter</button>
      <button>Filter</button>
    </>
  )
}

export default Filters
